# v0.1.0-alpha · ver0.1

## 这次包含什么

- Linux 原引擎动作接口、玩家观察、语义轨迹导入与机械状态比较。
- Mac arm64 被动录制 mod；可直接安装 DLL+JSON，不要求专用启动器。内部录制器修订号为0.2.3，与仓库版本独立。
- Mac 可选独立游戏/profile 准备和启动入口。
- Linux 工作材料回收与空间检查；保留失败、动作、观察、源码和存档证据。
- MIT 自有代码、上游归属、两端安装与跨平台衔接说明。

## 验证范围

| 检查 | 范围 |
| --- | --- |
| Linux 原候选真实运行 | 固定工具链离线构建；Continue 商店→地图→火堆/网格，12输入/12后继，机械与该短段时机一致，退出及原件保护检查通过 |
| Mac 本次构建 | 原生 arm64 .NET9 离线构建，无游戏依赖打包，无调试符号/本机源码路径随DLL分发 |
| Mac drop-in 启动 | 原版Harmony、无STS2录制器环境变量；在普通独立app/profile测试副本中关闭Steam运行，自动初始化、写出20个启动事件，0录制错误、0玩家动作，主菜单完整加载，退出0 |
| Mac 新准备入口 | 普通复制并核对app/profile，设置独立用户目录，拒绝覆盖、链接输入和不匹配版本；本次并未在原Steam安装部署 |
| 无游戏检查 | 16项Linux协议/比较及回收边界测试、3项Mac文件隔离/不完整导出测试通过；源码白名单及实际暂存内容核对 |

Mac此次没有执行新的完整鼠标片段；普通Steam启动、首次mod同意界面、Steam模式的全部存档位置仍需用户环境验证。一次图形查看工具额外启动了测试app并写入系统用户目录的设置/缓存，已停止且保留私人证据，不作为隔离通过依据；正式有界检查使用显式启动参数及受限写入范围。

Linux真实运行结果及历史证据细节见 [linux/RELEASE.md](linux/RELEASE.md)。后续目录重组及磁盘回收修改没有重跑这条真实流程，合成检查与历史证据分开。

## 已知限制

- 仅固定游戏版本。Linux还固定工具链及隔离用户态；不等于任意Linux发行版都能直接跑。
- 历史157输入跨幕机械比较通过，77次拖牌存在输入时机差异；不是整局保真。
- 通用Mac导入仍拒绝旧未归属`next_act`、未知UI输入及缺失后继。不会搜索未来checkpoint补动作。
- 占卜不支持；其他复杂机制只有部分分支验证。多药水popup过滤已编译，未做专项真实交互回归。
- 完整SL、跨进程恢复、全部角色/难度、HPC、大规模训练和稳定RL延期。
- 最小学习示例仅验证过20样本、8更新、恢复及两次独立评估，不声称胜率改善。
- 普通mod沿用游戏保存语义；独立启动器的profile隔离不是普通mod的默认属性。

源码和预编译mod均不含游戏、存档、真实轨迹或私人日志。没有为准备本版提交OpenAI福利申请。
